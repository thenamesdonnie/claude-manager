# Every Claude session in the launcher shares this rail. High is the throttle-and-reclaim point,
# Max is where the kernel kills the biggest process. Raise both on a roomier box.
[Service]
MemoryHigh=6G
MemoryMax=8G
MemorySwapMax=2G
